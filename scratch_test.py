import os, json; from groq import Groq; from dotenv import load_dotenv; load_dotenv(); client = Groq(); f = open(r"data\audio\original\First Python Program Print Hello World Explained in Hindi l Python Tutorial For Beginners.wav", "rb"); resp = client.audio.transcriptions.create(model="whisper-large-v3", file=f, response_format="verbose_json", timestamp_granularities=["segment", "word"]); print(json.dumps(resp.model_dump(), indent=2)[:2000])
