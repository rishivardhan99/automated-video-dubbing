# Optional dependencies for multi-speaker diarization (stretch goal).
# Only install these if you want to use SPEAKER_MODE=diarized.
# The core pipeline (SPEAKER_MODE=single) does NOT need these.
torch
torchaudio
pyannote.audio<4.0.0
